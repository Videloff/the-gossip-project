<!DOCTYPE html>
<html>
  <body class="bk_show">
    <center>
      <br>
      <div class="card border-secondary mb-3" style="max-width: 900px; opacity: 75%">
        <div class="card-header h3">Se connecter :</div>
          <div class="card-body">
            <%= form_tag('/sessions', method: 'post') do %>
              <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
              <br>
              <%= text_field_tag :email, nil, placeholder: "Votre email", size: 30 %>
              <br><br>
                <%= password_field_tag :password, nil, placeholder: "Mot de passe", size: 30 %>
                <br><br>
                <%= submit_tag "Connexion", class: "button btn btn-outline-info btn-dark" %>
                <br><br>
                <div class="align-links"><p class="opacity"> Pas encore inscrit ?</p><div style="margin-left: 4px">  </div><%= link_to "Créer un compte", new_user_path, :class=>"my_link" %></div>
            <% end %>
          </div>
        </div>
      </div>
    </center>
  </body>
</html>